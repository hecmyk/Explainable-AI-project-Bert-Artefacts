"""LangChain tools exposing DQ artifacts and the proposal queue to the agent."""
import json
import time
from datetime import datetime
from pathlib import Path

import pandas as pd
from langchain_core.tools import tool

from dq.bootstrap import PROJECT_ROOT, load_config
from dq import proposals
from dq import loader

_FENGO_CACHE = {}


def _artifacts_dir():
    """Path to the artifacts directory."""
    return Path(PROJECT_ROOT) / "artifacts"


def _log(tool_name, args, result, t0):
    """Append a tool call record to artifacts/agent_calls.jsonl and return result unchanged."""
    duration_ms = int((time.time() - t0) * 1000)
    log_path = _artifacts_dir() / "agent_calls.jsonl"
    log_path.parent.mkdir(parents=True, exist_ok=True)
    with open(log_path, "a") as f:
        f.write(json.dumps({
            "ts": datetime.now().isoformat(timespec="seconds"),
            "tool": tool_name,
            "args": args,
            "duration_ms": duration_ms,
            "result_preview": str(result)[:200],
        }) + "\n")
    return result


def _load_rules():
    """Load rules.json, always returning a list of specs (handles both dict-with-rules and list-directly)."""
    data = json.loads((_artifacts_dir() / "rules.json").read_text())
    if isinstance(data, dict):
        return data.get("rules", [])
    return data


def _load_run_rules_df():
    return pd.read_csv(_artifacts_dir() / "run_rules.csv")


def _load_run_records_df():
    return pd.read_csv(_artifacts_dir() / "run_records.csv")


def _load_dqi_df():
    return pd.read_csv(_artifacts_dir() / "run_dqi.csv")


def _get_fengo():
    """Load and cache (df_fengo, df_bdd, df_dqrc) via loader.load(cfg)."""
    if "data" not in _FENGO_CACHE:
        cfg = load_config()
        _FENGO_CACHE["data"] = loader.load(cfg)
    return _FENGO_CACHE["data"]


def _spec_columns(spec):
    """Extract the list of target columns from a spec, handling both scalar and list forms."""
    cols = spec.get("target_columns") or spec.get("column") or spec.get("target_column")
    if isinstance(cols, str):
        return [cols]
    return cols or []


def _spec_type(spec):
    """Extract the check type from a spec."""
    if isinstance(spec.get("check"), dict):
        return spec["check"].get("type")
    return spec.get("type") or spec.get("rule_type")


# --- Read tools ---

@tool
def list_rules(column: str = "", check_type: str = "") -> str:
    """List active rules, optionally filtered by column (substring) and check type."""
    t0 = time.time()
    rules = _load_rules()
    out = []
    for spec in rules:
        cols = _spec_columns(spec)
        typ = _spec_type(spec)
        if column and not any(column.lower() in str(c).lower() for c in cols):
            continue
        if check_type and check_type.lower() != str(typ).lower():
            continue
        out.append({
            "rule_id": spec.get("rule_id") or spec.get("id") or spec.get("name"),
            "target_columns": cols,
            "check_type": typ,
            "review_status": spec.get("review_status"),
            "source_text": spec.get("source_text") or spec.get("description"),
        })
    truncated = len(out) > 50
    out = out[:50]
    result = json.dumps(out, default=str)
    if truncated:
        result += "\n... (truncated)"
    return _log("list_rules", {"column": column, "check_type": check_type}, result, t0)


@tool
def list_uncovered_columns() -> str:
    """List FENGO columns not covered by any rule."""
    t0 = time.time()
    df_fengo, _, _ = _get_fengo()
    fengo_cols = set(df_fengo.columns)
    covered = set()
    for spec in _load_rules():
        for c in _spec_columns(spec):
            covered.add(str(c))
    uncovered = sorted(c for c in fengo_cols if c not in covered)
    result = json.dumps(uncovered)
    return _log("list_uncovered_columns", {}, result, t0)


@tool
def list_columns_without_definition() -> str:
    """List FENGO columns with no entry (by Name) in the Business Data Dictionary."""
    t0 = time.time()
    df_fengo, df_bdd, _ = _get_fengo()
    defined = {str(n).strip().lower() for n in df_bdd["Name"].dropna()}
    missing = [c for c in df_fengo.columns if str(c).strip().lower() not in defined]
    result = json.dumps(missing)
    return _log("list_columns_without_definition", {}, result, t0)


@tool
def profile_column(column: str) -> str:
    """Profile a FENGO column: nulls, uniques, top values, dtype, samples."""
    t0 = time.time()
    df_fengo, _, _ = _get_fengo()
    s = df_fengo[column]
    n_total = int(len(s))
    n_null = int(s.isna().sum())
    non_null = s.dropna()
    profile = {
        "n_total": n_total,
        "n_null": n_null,
        "pct_null": round(n_null / n_total * 100, 2) if n_total else 0.0,
        "n_unique": int(s.nunique(dropna=True)),
        "top_5_values": {str(k): int(v) for k, v in s.value_counts().head(5).items()},
        "dtype": str(s.dtype),
        "sample_values": [str(v) for v in non_null.drop_duplicates().head(5).tolist()],
    }
    result = json.dumps(profile)
    return _log("profile_column", {"column": column}, result, t0)


@tool
def get_failures(rule_id: str, limit: int = 10) -> str:
    """Return up to `limit` violation records for a rule."""
    t0 = time.time()
    df = _load_run_records_df()
    id_col = "rule_id" if "rule_id" in df.columns else df.columns[0]
    rows = df[df[id_col] == rule_id].head(limit)
    result = json.dumps(rows.to_dict(orient="records"), default=str)
    return _log("get_failures", {"rule_id": rule_id, "limit": limit}, result, t0)


@tool
def list_failures_by_column(column: str, limit: int = 20) -> str:
    """Return up to `limit` failing records for a given column across all rules."""
    t0 = time.time()
    try:
        df = _load_run_records_df()
    except (FileNotFoundError, pd.errors.EmptyDataError):
        return _log("list_failures_by_column", {"column": column}, "No run records yet.", t0)
    col_name = "column" if "column" in df.columns else None
    if col_name is None:
        result = "No 'column' field in run_records.csv."
        return _log("list_failures_by_column", {"column": column}, result, t0)
    rows = df[df[col_name] == column].head(limit)
    result = json.dumps(rows.to_dict(orient="records"), default=str)
    return _log("list_failures_by_column", {"column": column, "limit": limit}, result, t0)


@tool
def get_record(record_id: str) -> str:
    """Return all FENGO columns for a single record identified by its record_key value."""
    t0 = time.time()
    df_fengo, _, _ = _get_fengo()
    cfg = load_config()
    key = cfg["data"]["record_key"]
    row = df_fengo[df_fengo[key].astype(str) == str(record_id)]
    if row.empty:
        result = f"No record found with {key}={record_id}."
    else:
        r = row.iloc[0].to_dict()
        r = {k: (None if pd.isna(v) else str(v)) for k, v in r.items()}
        result = json.dumps(r, default=str)
    return _log("get_record", {"record_id": record_id}, result, t0)


@tool
def find_similar_records(column: str, value: str, limit: int = 5) -> str:
    """Return up to `limit` records that share the same value in the given column.

    Useful to justify a suggested correction by pointing to peers.
    """
    t0 = time.time()
    df_fengo, _, _ = _get_fengo()
    cfg = load_config()
    key = cfg["data"]["record_key"]
    match = df_fengo[df_fengo[column].astype(str) == str(value)].head(limit)
    if match.empty:
        result = f"No records found with {column}={value}."
    else:
        cols_out = [key, column] + [c for c in df_fengo.columns
                                    if c not in (key, column)][:5]
        result = json.dumps(match[cols_out].to_dict(orient="records"), default=str)
    return _log("find_similar_records",
                {"column": column, "value": value, "limit": limit}, result, t0)


@tool
def get_last_run_summary() -> str:
    """Summarize the last pipeline run: global DQI, per-dimension DQI, worst rules, rule count."""
    t0 = time.time()
    try:
        dqi = _load_dqi_df()
    except FileNotFoundError:
        return _log("get_last_run_summary", {}, "No run yet.", t0)
    run_rules = _load_run_rules_df()

    dqi_global = None
    dqi_by_dimension = {}
    if "dimension" in dqi.columns:
        val_col = next((c for c in dqi.columns if c.lower() in ("dqi", "score", "value", "rate")),
                       dqi.columns[-1])
        for _, row in dqi.iterrows():
            dim = str(row["dimension"])
            if dim.lower() in ("global", "overall", "all"):
                dqi_global = row[val_col]
            else:
                dqi_by_dimension[dim] = row[val_col]

    worst = []
    for cand in ("pass_rate", "rate", "failed"):
        if cand in run_rules.columns:
            asc = cand != "failed"  # for "failed" we want highest first
            cols = [c for c in ("rule_id", "column", "dimension", cand) if c in run_rules.columns]
            worst = run_rules.sort_values(cand, ascending=asc).head(5)[cols].to_dict(orient="records")
            break

    summary = {
        "dqi_global": dqi_global,
        "dqi_by_dimension": dqi_by_dimension,
        "worst_5_rules": worst,
        "n_rules_run": int(len(run_rules)),
    }
    result = json.dumps(summary, default=str)
    return _log("get_last_run_summary", {}, result, t0)


# --- Write tools ---

@tool
def propose_add_rule(spec_json: str, rationale: str) -> str:
    """Propose adding a new rule. spec_json is a JSON string with target_columns, check{type,params}, source_text."""
    t0 = time.time()
    spec = json.loads(spec_json)
    prop = proposals.add_proposal("add_rule", {"spec": spec}, rationale)
    result = f"Proposal {prop['id']} created (add_rule)."
    return _log("propose_add_rule", {"rationale": rationale}, result, t0)


@tool
def propose_modify_rule(rule_id: str, new_spec_json: str, rationale: str) -> str:
    """Propose modifying an existing rule."""
    t0 = time.time()
    new_spec = json.loads(new_spec_json)
    prop = proposals.add_proposal("modify_rule", {"rule_id": rule_id, "new_spec": new_spec}, rationale)
    result = f"Proposal {prop['id']} created (modify_rule)."
    return _log("propose_modify_rule", {"rule_id": rule_id}, result, t0)


@tool
def propose_delete_rule(rule_id: str, rationale: str) -> str:
    """Propose deleting a rule."""
    t0 = time.time()
    prop = proposals.add_proposal("delete_rule", {"rule_id": rule_id}, rationale)
    result = f"Proposal {prop['id']} created (delete_rule)."
    return _log("propose_delete_rule", {"rule_id": rule_id}, result, t0)


@tool
def propose_definition(column: str, name: str, definition: str, example: str, rationale: str) -> str:
    """Propose a Business Data Dictionary definition for a column."""
    t0 = time.time()
    payload = {"column": column, "name": name, "definition": definition, "example": example}
    prop = proposals.add_proposal("definition", payload, rationale)
    result = f"Proposal {prop['id']} created (definition)."
    return _log("propose_definition", {"column": column}, result, t0)


@tool
def propose_remediation(record_id: str, column: str, suggested_value: str,
                        source: str, confidence: float, remediation_type: str,
                        rationale: str) -> str:
    """Propose a corrected value for a failing record.

    remediation_type must be one of:
    - document_based: correction sourced from an official document (contract, ID card, registry)
    - business_decision: requires a business owner call (ambiguous, no ground truth)
    - upstream_fix: root cause is in a source system, correction here is only a workaround
    - format_normalization: value is correct but format is wrong (whitespace, casing, encoding)
    - inferred_from_context: correction inferred from other columns of the same record or from dataset patterns
    """
    t0 = time.time()
    ALLOWED = {"document_based", "business_decision", "upstream_fix",
               "format_normalization", "inferred_from_context"}
    if remediation_type not in ALLOWED:
        result = f"Error: remediation_type must be one of {sorted(ALLOWED)}."
        return _log("propose_remediation", {"record_id": record_id}, result, t0)
    payload = {"record_id": record_id, "column": column,
               "suggested_value": suggested_value, "source": source,
               "confidence": confidence, "remediation_type": remediation_type}
    prop = proposals.add_proposal("remediation", payload, rationale)
    result = f"Proposal {prop['id']} created (remediation, type={remediation_type})."
    return _log("propose_remediation", {"record_id": record_id, "column": column}, result, t0)


ALL_TOOLS = [list_rules, list_uncovered_columns, list_columns_without_definition,
             profile_column, get_failures, list_failures_by_column,
             get_record, find_similar_records,
             propose_add_rule, propose_modify_rule, propose_delete_rule,
             propose_definition, propose_remediation]









"""System prompt for the DQ agent."""

SYSTEM_PROMPT = """You are a Data Quality assistant working on a financial dataset (FENGO synthetic client records).
You have four responsibilities:

1. OPTIMIZE the current rule catalog: find redundancies, conflicts, or dead rules and propose deletions or modifications.
2. DISCOVER new rules: for columns that have no rule or where the data profile suggests missing checks (high null rate, invalid patterns, etc.), propose new rules.
3. DRAFT definitions for columns that have no entry in the Business Data Dictionary.
4. REMEDIATE disqualities: for records that fail rules, propose a corrected value with source, evidence, confidence, and remediation type.

Your workflow depends on the task:
- Rule optimization/discovery: use list_rules, list_uncovered_columns, profile_column.
- Definitions: use list_columns_without_definition and profile_column.
- Remediation: use list_failures_by_column (all failures on a column) or get_failures (failures of one rule), then get_record to inspect the full context of a failing record, and find_similar_records to look for peers with a plausible value.

Only after gathering evidence, use the propose_* tools. Every proposal MUST include a clear rationale grounded in what you observed.

For remediation proposals, you MUST classify the correction with remediation_type, one of:
- document_based: from an official document (contract, ID, registry)
- business_decision: requires a business owner call
- upstream_fix: root cause is in a source system
- format_normalization: value correct but format wrong (whitespace, casing)
- inferred_from_context: inferred from other columns or dataset patterns

Set confidence between 0 and 1. Be honest: 0.3 for pure guesses, 0.9+ only when strong evidence.

NEVER apply changes directly. All propose_* tools create pending proposals that a human will review.

Rule spec format (for propose_add_rule and propose_modify_rule):
{
  "target_columns": ["<FENGO column name>"],
  "check": {"type": "not_null | allowed_values | regex | comparison | conditional | cross_field_map | unique", "params": {...}},
  "source_text": "<one-line human-readable description>"
}

Be concise. Do not propose more than 3 changes per user request unless explicitly asked.
"""














PROMPT_REMEDIATE = """Propose remediations for failing records on a specific column.

Replace <COLUMN_NAME> with the target FENGO column before sending.

Steps:
1. Use list_failures_by_column with column="<COLUMN_NAME>" to see the failing records.
2. Pick up to 3 records to remediate.
3. For each record, use get_record to see its full context (other columns of the same entity), and optionally find_similar_records on related columns to find plausible correction sources.
4. For each remediation, use propose_remediation with:
   - suggested_value: the corrected value you propose
   - source: where the suggestion comes from (e.g., "inferred from Legal Entity Name", "dataset peer records", "requires business owner input")
   - confidence: 0-1 (be conservative)
   - remediation_type: document_based | business_decision | upstream_fix | format_normalization | inferred_from_context
   - rationale: cite the specific evidence you found

Do not propose more than 3 remediations."""










        c1, c2, c3, c4 = st.columns(4)
        c1.button("1. Optimize rules", on_click=_set_composer,
                  args=(PROMPT_OPTIMIZE,), use_container_width=True)
        c2.button("2. Discover rules", on_click=_set_composer,
                  args=(PROMPT_DISCOVER,), use_container_width=True)
        c3.button("3. Draft definitions", on_click=_set_composer,
                  args=(PROMPT_DEFINITIONS,), use_container_width=True)
        c4.button("4. Remediate", on_click=_set_composer,
                  args=(PROMPT_REMEDIATE,), use_container_width=True)














