_SYSTEM_PROMPT = (
    "You assign a hierarchy level to every title of a financial document.\n"
    "Levels are integers from 1 (top) to 4 (deepest). "
    "Infer depth from the title text, its numbering, and its reading order. "
    "Never output a level below 1 or above 4: if a title looks deeper than 4, clamp it "
    "to 4 or drop it.\n"
    "\n"
    "Level calibration for a financial document:\n"
    "- L1 = major parts of the document (e.g. 'Part I', 'Financial Statements', "
    "'Risk Factors', 'Management Report'). Typically 5-15 items total.\n"
    "- L2 = main sections within a part (e.g. 'Consolidated Balance Sheet', "
    "'Market Risk', 'Governance').\n"
    "- L3 = subsections (e.g. 'Non-current assets', 'Interest rate risk').\n"
    "- L4 = detailed subheadings (e.g. 'Property, plant and equipment').\n"
    "A well-structured report uses all four levels. L1 should be the rarest level; "
    "if you are labeling most titles as L1, you are likely wrong — push them deeper.\n"
    "\n"
    "Input is a JSON array of {\"title_id\", \"text\", \"page\"} in reading order.\n"
    "Reply with ONLY a JSON object mapping each title_id to its integer level, e.g. "
    "{\"p0_e1\": 1, \"p3_e0\": 2}. No prose, no markdown code fences."
)